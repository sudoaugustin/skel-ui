---
title: Rendering List
description: Skel UI has a built-in utility for generating placeholders, which is very useful for rendering lists.
---

import Portal from "app/Portal";
import { PostCardList } from "@skel-ui/react/$components";

<Portal codesandbox="https://codesandbox.io/p/sandbox/rendering-list-34rj8q" className="[&>div]:max-h-[32rem] lg:[&>div]:max-h-[44rem]">
  <PostCardList />
</Portal>

```json doc-gen:file
{
  "file": "./codes/snippets/PostCardList.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": ""
  }
}
```

## `generatePlaceholder`

```js
const placeholderUsers = generatePlaceholder(length, primaryKey);
```

### Parameters

| Name         | Type     | Description                                                         |
| ------------ | -------- | ------------------------------------------------------------------- |
| `length`     | `number` | The length of placeholder                                           |
| `primaryKey` | `string` | Name of the attribute that will be unique and will be used as `key` |
